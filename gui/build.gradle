ext {
    moduleName = 'se.lovebrandefelt.graphingcalculator.gui'
}

jar {
    manifest {
        attributes 'Main-Class': 'se.lovebrandefelt.graphingcalculator.gui.Gui'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}